# Test dependencies. Includes the runtime set so both are resolved together and
# CI can never install a different version of a shared package than production.
#
#   uv pip compile server/requirements-dev.in --universal \
#     --python-version 3.10 --generate-hashes \
#     -o server/requirements-dev.txt
#
# CI installs requirements-dev.txt only; the host installs requirements.txt only.

-r requirements.in

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
aiosqlite>=0.19.0

# Linting. The backend had none: the frontend got ESLint and Stylelint while
# Python got nothing, and ruff flags the undefined-attribute class of bug that
# put five broken auth routes into production.
ruff>=0.6.0

# Dependency advisories. CI runs it against server/requirements.txt - the lock
# the production host installs - as a blocking step; nothing did before, so a
# vulnerable pin could ship with every check green.
pip-audit>=2.7.0
